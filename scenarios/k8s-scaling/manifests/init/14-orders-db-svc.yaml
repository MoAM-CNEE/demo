apiVersion: kubernetes.crossplane.io/v1alpha2
kind: Object
metadata:
  name: 14-orders-db-svc
spec:
  forProvider:
    manifest:
      apiVersion: v1
      kind: Service
      metadata:
        name: orders-db
        labels:
          name: orders-db
        namespace: sock-shop
      spec:
        ports:
        - port: 27017
          targetPort: 27017
        selector:
          name: orders-db
  providerConfigRef:
    name: provider-config-kubernetes
