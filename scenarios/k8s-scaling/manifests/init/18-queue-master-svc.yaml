apiVersion: kubernetes.crossplane.io/v1alpha2
kind: Object
metadata:
  name: 18-queue-master-svc
spec:
  forProvider:
    manifest:
      apiVersion: v1
      kind: Service
      metadata:
        name: queue-master
        annotations:
          prometheus.io/scrape: 'true'
        labels:
          name: queue-master
        namespace: sock-shop
      spec:
        ports:
        - port: 80
          targetPort: 80
        selector:
          name: queue-master
  providerConfigRef:
    name: provider-config-kubernetes
